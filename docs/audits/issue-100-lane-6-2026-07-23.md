# Issue 100 — Lane 6 local evidence

Base: `e8d7e77a58165e963f8b4c447092ec438bad74ed`
Checkout: `codex/issue-100-complete-20260724`
Scope: installed Code E2E, installed lifecycle E2E, multisession evidence tests.

## MEASURED results

- `python -m pytest -q scripts/tests/test_installed_code_e2e.py scripts/tests/test_installed_lifecycle_e2e.py scripts/tests/test_multisession_e2e.py` — **18 passed, 8 subtests passed**.
- `python scripts/installed_code_e2e.py --fixture` — passed as `hermetic_fixture_non_proof`; all four surfaces, fail-closed dependency gates, cancel/reconcile, advisory replay, Runtime effects, Prototype-First read/write, atomic flags, and repeated idempotent artifact write were exercised. The receipt reported `scenario_count=28` and `prototype_artifact_idempotent_retry=true`.
- `python scripts/release/installed_lifecycle_e2e.py --fixture` — passed clean install, inactive canary, upgrade, rollback, digest checks, and deterministic observations. The receipt keeps Windows/macOS/production-release metrics as `null` with reasons.
- `git diff --check` — passed.
- Strict boundary scan run before this lane patch — `findings=7455`, `pending_paths=555`, `unknown=0`, `expired=0`; it remained fail-closed. No scanner/config/Rust file was changed.

## MEASURED safety observations

- Windows subprocesses now use explicit pipe/standard-input handling; POSIX shebang fixtures are invoked through an explicit shell interpreter when available.
- An injected activation interruption removes the read-only failed staging prefix, and a retry from a clean prefix passes.
- Receipts assert absence of `HOME`, `TOKEN`, `Authorization`, and `Bearer ` values.
- `gitleaks 8.30.1` scanned the checkout and exited with 2 findings; this lane does not claim a clean secret scan because the captured summary did not identify the finding paths.

## UNVERIFIED / blocked

- `test_benchmark_hbp_events.py` is pre-existing red outside this write-set: `2 failed, 2 passed` because the imported `benchmark_snake` module has no `write_events` attribute. It was not changed.
- A fresh HBI-specific run was not repeated in this stop-requested round; HBI conformance remains `UNVERIFIED` here.
- The fixture runs are not proof of independently installed AgentHost/Runtime packages. Cross-platform package provenance, real upgrade/rollback, strict repository zero-findings, and GitHub Actions remain unverified/blocked (billing).
- This receipt makes no issue-closure claim.
