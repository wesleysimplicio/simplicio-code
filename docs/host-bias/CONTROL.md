# Host control text (UWP control)

Use the host's normal workflow for code tasks and preserve its ordinary
approval, execution, and delivery behavior.

This control text intentionally does not mention Simplicio. It is used only
as the neutral comparison arm of the adoption experiment.
